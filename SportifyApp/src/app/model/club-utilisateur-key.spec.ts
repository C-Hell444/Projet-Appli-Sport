import { ClubUtilisateurKey } from './club-utilisateur-key';

describe('ClubUtilisateurKey', () => {
  it('should create an instance', () => {
    expect(new ClubUtilisateurKey()).toBeTruthy();
  });
});
