import { EvenementUtilisateurKey } from './evenement-utilisateur-key';

describe('EvenementUtilisateurKey', () => {
  it('should create an instance', () => {
    expect(new EvenementUtilisateurKey()).toBeTruthy();
  });
});
