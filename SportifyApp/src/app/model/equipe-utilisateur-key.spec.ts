import { EquipeUtilisateurKey } from './equipe-utilisateur-key';

describe('EquipeUtilisateurKey', () => {
  it('should create an instance', () => {
    expect(new EquipeUtilisateurKey()).toBeTruthy();
  });
});
